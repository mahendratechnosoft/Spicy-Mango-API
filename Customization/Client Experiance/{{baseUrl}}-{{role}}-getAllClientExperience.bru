meta {
  name: {{baseUrl}}/{{role}}/getAllClientExperience
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/{{role}}/getAllClientExperience
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}
