meta {
  name: {{baseUrl}}/{{role}}/getAllFeedback
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/{{role}}/getAllFeedback
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
