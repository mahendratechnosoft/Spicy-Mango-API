meta {
  name: {{baseUrl}}/{{role}}/getHeroSection
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/{{role}}/getHeroSection
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
