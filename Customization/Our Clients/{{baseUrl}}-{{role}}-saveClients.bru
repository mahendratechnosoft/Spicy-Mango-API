meta {
  name: {{baseUrl}}/{{role}}/saveClients
  type: http
  seq: 1
}

put {
  url: {{baseUrl}}/{{role}}/saveClients
  body: json
  auth: inherit
}

body:json {
  {
    "imageUrl": [
      "kartik1",
      "check"
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
