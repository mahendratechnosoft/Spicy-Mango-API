meta {
  name: {{baseUrl}}/{{role}}/saveFooterLocationLink
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/{{role}}/saveFooterLocationLink
  body: json
  auth: inherit
}

body:json {
  {
    "type":"VILLA",
    "locationNames":[
      "Alibag",
      "Banglore",
      "Mumbai"
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
