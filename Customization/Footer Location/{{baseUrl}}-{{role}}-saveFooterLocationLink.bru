meta {
  name: {{baseUrl}}/{{role}}/saveFooterLocationLink
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/{{role}}/saveFooterLocationLink
  body: json
  auth: inherit
}

body:json {
  {
    "type": "RESORT",
    "locationNames": [
      "Alibag",
      "Bangalore",
      "Mumbai",
      "Kokan"
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
