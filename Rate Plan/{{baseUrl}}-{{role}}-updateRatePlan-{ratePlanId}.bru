meta {
  name: {{baseUrl}}/{{role}}/updateRatePlan/{ratePlanId}
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/{{role}}/updateRatePlan/{ratePlanId}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
