meta {
  name: {{baseUrl}}/{{role}}/getPinnedProperties/{sectionName}
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/{{role}}/getPinnedProperties/OUR_VILLAS
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
