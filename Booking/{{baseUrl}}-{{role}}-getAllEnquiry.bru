meta {
  name: {{baseUrl}}/{{role}}/getAllEnquiry
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/{{role}}/getAllBooking?page=0&size=10&search=ritesh
  body: none
  auth: bearer
}

params:query {
  page: 0
  size: 10
  search: ritesh
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
