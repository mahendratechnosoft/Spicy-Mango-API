meta {
  name: {{baseUrl}}/{{role}}/getAllBooking
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/{{role}}/getAllBooking?page=0&size=10&sort=customerEmail,desc
  body: none
  auth: bearer
}

params:query {
  page: 0
  size: 10
  sort: customerEmail,desc
  ~search: ritesh
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
