meta {
  name: {{baseUrl}}/{{role}}/getAllFooterLocationLink
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/{{role}}/getAllFooterLocationLink
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}
