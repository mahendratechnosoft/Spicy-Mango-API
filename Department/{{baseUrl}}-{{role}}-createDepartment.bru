meta {
  name: {{baseUrl}}/{{role}}/createDepartment
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/{{role}}/createDepartment
  body: json
  auth: inherit
}

body:json {
  {
    "name":"Department_one"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
