meta {
  name: {{baseUrl}}/{{role}}/test
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/{{role}}/test
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
