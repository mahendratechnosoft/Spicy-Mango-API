meta {
  name: {{baseUrl}}/{{role}}/syncPropertiesFromStayFlexi
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/{{role}}/syncPropertiesFromStayFlexi
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
