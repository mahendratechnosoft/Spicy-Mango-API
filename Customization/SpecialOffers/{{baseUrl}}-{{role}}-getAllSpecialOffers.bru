meta {
  name: {{baseUrl}}/{{role}}/getAllSpecialOffers
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/{{role}}/getAllSpecialOffers
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
