meta {
  name: {{baseUrl}}/{{role}}/getRoomImagesByRoomId/{roomId}
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/{{role}}/getRoomImagesByRoomId/{roomId}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}
