meta {
  name: {{baseUrl}}/{{role}}/getRoomDropdownByStayflexiPropertyId
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/{{role}}/getRoomDropdownByStayflexiPropertyId?stayflexiPropertyId=31068
  body: none
  auth: inherit
}

params:query {
  stayflexiPropertyId: 31068
}

settings {
  encodeUrl: true
  timeout: 0
}
