meta {
  name: {{baseUrl}}/{{role}}/getDailyRatesForRoom/{stayflexiRoomTypeId}
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/{{role}}/getDailyRatesForRoom/{stayflexiRoomTypeId}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
