meta {
  name: {{baseUrl}}/{{role}}/test
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/{{role}}/test
  body: none
  auth: bearer
}

auth:bearer {
  token: {{bearerToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
